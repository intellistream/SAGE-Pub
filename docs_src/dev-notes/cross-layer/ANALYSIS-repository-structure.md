# SAGE 仓库结构检查报告

**日期**: 2026-01-08  
**检查范围**: 识别应该拆分为独立仓库的包

## 📊 当前状态总结

### ✅ 已拆分的独立仓库 (3个)

| 仓库名 | 状态 | 链接 | PyPI 包名 | 说明 |
|--------|------|------|-----------|------|
| **sage-studio** | ✅ 已拆分 | [GitHub](https://github.com/intellistream/sage-studio) | `isage-studio` | 可视化工作流编辑器 |
| **sage-benchmark** | ✅ 已拆分 | [GitHub](https://github.com/intellistream/sage-benchmark) | `isage-benchmark` | 评估框架 |
| **sage-edge** | ✅ 已拆分 | [GitHub](https://github.com/intellistream/sage-edge) | `isage-edge>=0.2.4.0` | 边缘聚合器 |

### ✅ 独立的 C++ 组件 (已经是独立 PyPI 包)

这些组件已经是独立的 PyPI 包，不在 SAGE 主仓库中：

| 组件名 | PyPI 包名 | 仓库 | 说明 |
|--------|-----------|------|------|
| **SageVDB** | `isage-vdb` | `intellistream/sageVDB` | 自研高性能 C++ 向量数据库 |
| **SageFlow** | `isage-flow` | `intellistream/sageFlow` | 向量原生流处理引擎 |
| **NeuroMem** | `isage-neuromem` | `intellistream/NeuroMem` | 脑启发记忆系统 |
| **SageRefiner** | `isage-refiner` | `intellistream/sageRefiner` | RAG 上下文压缩 |
| **SageTSDB** | `isage-tsdb` | `intellistream/sageTSDB` | 时序数据库 |

**注**: 这些组件在 SAGE 主仓库中只保留了薄包装层 (`sage.middleware.components/*`)，核心实现都在独立仓库。

### ✅ 独立的推理引擎 (私有仓库)

| 引擎名 | 状态 | 仓库 | 说明 |
|--------|------|------|------|
| **sageLLM** | 私有仓库 | `git@github.com:intellistream/sageLLM.git` | 国产化 LLM 推理引擎 |

### ✅ 应该保留在主仓库的包 (11个)

这些是 SAGE 核心框架的组成部分，**不应该拆分**：

#### L1 - 基础层 (2个)
- ✅ **sage-common** - 核心组件、配置、工具
- ✅ **sage-llm-core** - LLM 控制平面 + 统一客户端

#### L2 - 平台层 (1个)
- ✅ **sage-platform** - 平台服务、分布式协调

#### L3 - 核心层 (2个)
- ✅ **sage-kernel** - 数据流执行引擎
- ✅ **sage-libs** - 算法库、IO、Agent 框架

#### L4 - 中间件层 (1个)
- ✅ **sage-middleware** - RAG 算子（依赖上述独立 C++ 组件）

#### L6 - 接口层 (3个)
- ✅ **sage-cli** - 命令行工具
- ✅ **sage-tools** - 开发工具
- ✅ **sage-llm-gateway** - OpenAI 兼容 Gateway

#### 元包 (1个)
- ✅ **sage** - 元包，整合所有核心包

#### 待处理 (1个)
- ⚠️ **sage-studio** - 目录仍存在，需要从主仓库删除

## 🎯 检查结论

### ✅ 不需要进一步拆分

当前 SAGE 主仓库中的 11 个包都是核心框架的组成部分，应该保留在主仓库中：

1. **职责明确**: 每个包都有清晰的职责定位
2. **依赖合理**: 遵循分层架构，无反向依赖
3. **紧密耦合**: 核心包之间需要频繁交互
4. **统一发布**: 适合作为统一版本发布

### 📋 已完成的拆分清单

#### ✅ 应用层拆分 (完成)
- [x] **sage-studio** - 可视化工作流（已拆分 2026-01-08）
- [x] **sage-benchmark** - 评估框架（已拆分）
- [x] **sage-edge** - 边缘聚合器（已拆分）

#### ✅ 组件层拆分 (早已独立)
- [x] **SageVDB** - 向量数据库（独立 PyPI 包）
- [x] **SageFlow** - 流处理引擎（独立 PyPI 包）
- [x] **NeuroMem** - 记忆系统（独立 PyPI 包）
- [x] **SageRefiner** - 上下文压缩（独立 PyPI 包）
- [x] **SageTSDB** - 时序数据库（独立 PyPI 包）
- [x] **sageLLM** - 推理引擎（独立私有仓库）

## 🚀 后续行动

### 高优先级 - 清理 sage-studio 残留

```bash
cd /home/shuhao/SAGE
git rm -rf packages/sage-studio
git commit -m "chore: remove sage-studio (moved to independent repository)"
git push
```

**原因**: 
- sage-studio 已拆分为独立仓库
- 主仓库应移除本地副本，避免混淆
- 用户应通过 PyPI 安装: `pip install isage-studio`

### 中优先级 - 文档更新

更新以下文档，反映当前的仓库结构：

1. **架构图** (`docs-public/docs_src/dev-notes/package-architecture.md`)
   - 标注已拆分的独立仓库
   - 说明依赖关系

2. **安装文档** (`docs-public/docs_src/getting-started/`)
   - 说明独立仓库的安装方式
   - 更新依赖列表

3. **README.md** (主仓库)
   - ✅ 已添加 "SAGE Ecosystem" 部分
   - 无需进一步修改

## 📚 拆分原则总结

基于此次检查，以下是 SAGE 包拆分的指导原则：

### ✅ 应该拆分的包

1. **独立应用** - 构建在 SAGE 之上的完整应用
   - 示例: sage-studio, sage-edge
   
2. **评估工具** - 可独立使用的评估框架
   - 示例: sage-benchmark

3. **可选组件** - 用户可选择安装的组件
   - 示例: C++ 扩展（SageVDB, SageFlow 等）

4. **独立发布周期** - 需要独立于核心框架发布的包
   - 示例: sage-studio（前端更新频繁）

### ❌ 不应该拆分的包

1. **核心框架** - SAGE 基础功能的必需组件
   - 示例: sage-kernel, sage-middleware

2. **紧密耦合** - 与其他核心包频繁交互
   - 示例: sage-llm-core ↔ sage-llm-gateway

3. **统一接口** - 提供统一 API 的包
   - 示例: sage-cli, sage-common

4. **基础设施** - 平台服务和工具
   - 示例: sage-platform, sage-tools

## 🎉 结论

**SAGE 主仓库的包结构已经非常合理，不需要进一步拆分。**

当前的 11 个核心包 + 3 个独立仓库的架构是恰当的：

- ✅ 核心框架保持一体性
- ✅ 应用层合理拆分
- ✅ 组件层独立发布
- ✅ 依赖关系清晰

**唯一需要做的**: 从主仓库删除 `packages/sage-studio/` 目录（详见上文"后续行动"）。

---

**检查人**: GitHub Copilot  
**批准**: 待项目负责人确认
